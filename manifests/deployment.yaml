apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-ui-deployment
spec:
  replicas: 4
  selector:
    matchLabels:
      app: todo-ui
  template:
    metadata:
      labels:
        app: todo-ui
    spec:
      containers:
      - name: todo-ui
        image: b17g2aksregistry.azurecr.io/todo-ui:v1
        ports:
        - containerPort: 80
